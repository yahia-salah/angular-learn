import { TitleCaseOfThePipe } from './title-case-of-the.pipe';

describe('TitleCaseOfThePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaseOfThePipe();
    expect(pipe).toBeTruthy();
  });
});
